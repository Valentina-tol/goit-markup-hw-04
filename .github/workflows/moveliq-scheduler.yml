name: MOVELIQ scheduler

on:
  schedule:
    - cron: "2-59/5 * * * *"
  workflow_dispatch:

jobs:
  run-moveliq-scheduler:
    runs-on: ubuntu-latest
    steps:
      - name: Publish due MOVELIQ posts
        run: |
          curl --fail --silent --show-error --retry 2 \
            "https://postonce.floot.app/_api/moveliq-cron-7c91"
